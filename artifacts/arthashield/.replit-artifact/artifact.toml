kind = "web"
previewPath = "/"
title = "ArthaShield"
version = "1.0.0"
id = "artifacts/arthashield"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20070

[services.development]
run = "pnpm --filter @workspace/arthashield run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/arthashield", "run", "build" ]
publicDir = "artifacts/arthashield/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20070"
BASE_PATH = "/"
